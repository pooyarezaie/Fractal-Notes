---
title: "عنوان یادداشت"
description: "یک جملهٔ کوتاه توصیفی برای موتور جست‌وجو و کارت‌های اشتراک‌گذاری."
image: "/assets/img/logo.png"
date: 2026-01-01            # publish date (YYYY-MM-DD) — set when the note goes live
last_modified_at: 2026-01-01  # bump on substantial edits
author: pooyarezaie         # key from _data/authors.yml; omit for the site owner
---

<!--
  Note template for Fractal Notes. Copy this file into a topic directory as
  <topic>/<kebab-case-name>.md, fill it in, then:
    1) register it in _data/site_index.yml (title + path, no leading slash, no .md)
    2) if the topic directory is new, add it to scripts/prerender-whitelist.json
  See AGENTS.md §4–§5 for the full workflow and style conventions.
  This whole comment block should be deleted from the final note.

  House style reminders:
    - Persian (RTL) body text; math in standard LaTeX with KaTeX delimiters.
    - Intuition first, formalism second. Short paragraphs. Warm, curious tone.
    - Inline math: $ ... $   ·   Display math: $$ ... $$ on their own lines.
    - Center images and always set width + height (see the example below).
    - Preview locally with `make dev` and confirm every formula renders.
-->

# عنوان اصلی
**یک زیرعنوانِ کوتاه و گیرا که ایدهٔ محوری را می‌رساند**

یک یا دو جملهٔ مقدماتی که خواننده را وارد فضای مسئله می‌کند و می‌گوید چرا این موضوع
جالب است.

---

## بخش نخست: شهود

توضیح شهودی، با یک مثالِ ملموس. مثالی از فرمول درون‌خطی مثل $a^2 + b^2 = c^2$ و یک
فرمولِ نمایشی:

$$
S(n) = 1 + 2 + \cdots + n = \frac{n(n+1)}{2}
$$

> یک اصلِ راهنما یا جملهٔ کلیدی را می‌توان این‌گونه برجسته کرد.

---

## بخش دوم: تصویر

<p align="center">
  <img src="/assets/img/your_diagram.png" alt="توضیح فارسیِ تصویر" width="320" height="320">
</p>

توضیح آنچه تصویر نشان می‌دهد.

---

## جمع‌بندی

یک بستنِ کوتاه: چه دیدیم و این ایده کجاها به کار می‌آید.
